# dependencies
/node_modules

# env files
.env*
!.env*.example

# generated vector store (rebuild with: npm run build:pipeline)
# NOTE: data/vector/ is committed on purpose so it deploys to Vercel,
# where the build:pipeline step never runs.
/data/chunks/

# generated graph store (rebuild with: npm run extract:graph)
/data/graph/

# next.js
/.next/
/next-env.d.ts

# misc
.DS_Store
*.tsbuildinfo
npm-debug.log*
